# DS60 tiny greeter fixture

This deliberately small package exists only for `python-pro-01`. It has a
`src/` layout, a console-script entry point, build metadata, dependency groups,
and no runtime dependency. Build it only into a temporary output directory.

